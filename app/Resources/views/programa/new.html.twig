{% extends 'base.html.twig' %}

{% block body %}
    <h1>Crear Programa</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-primary btn1"  value="Postular" />
    {{ form_end(form) }}

    <a href="{{ path('programa_index') }}" class="btn btn-danger btn2" role="button" >Cancelar</a>
{% endblock %}
