{% extends 'base.html.twig' %}

{% block body %}
    <h1>Fondolinea</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fondoLinea.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ fondoLinea.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fondolinea_index') }}" class="btn btn-info btn1" role="button" >Ver listado</a>
    <a href="{{ path('fondolinea_edit', { 'id': fondoLinea.id }) }}" class="btn btn-primary btn2" role="button">Editar</a>

    {{ form_start(delete_form) }}
        <input type="submit" value="Eliminar" class="btn btn-danger btn2" role="button">
    {{ form_end(delete_form) }}
{% endblock %}
